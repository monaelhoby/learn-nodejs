<nav class="navbar navbar-expand-lg navbar-light" style='background: #ddd !important; padding:0;margin-bottom:60px'>
    <div class="container-fluid">
      <a class="navbar-brand" href="#"><img src="/images/logo.jpg" width="80" height="80" /></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link <%= pageTitle === 'Home'? 'active' : ''%>" 
            aria-current="page" href="/">Home</a>
          </li>
          <% if(isAdmin) {%>
          <li class="nav-item ">
            <a class="nav-link <%= pageTitle === 'Add Product'? 'active' : ''%>" 
            href="/admin/add">Add Product</a>
          </li>
          <% } %>
        </ul>
        <div class="d-flex">
          <ul class="navbar-nav mb-2 mb-lg-0">
            <% if(isUser) {%>
              <li class="nav-item">
                <a class="nav-link <%= pageTitle === 'Cart'? 'active' : ''%>" href="/cart">Cart</a>
              </li>
              <form action="/logout" method="POST">
                <input type="submit" value="Logout" class="btn btn-danger"/>
              </form>
            <% } else { %>
            <li class="nav-item ">
              <a class="nav-link <%= pageTitle === 'Login'? 'active' : ''%>" href="/login">Login</a>
            </li>
            <li class="nav-item ">
              <a class="nav-link <%= pageTitle === 'Signup'? 'active' : ''%>" href="/signup">Signup</a>
            </li>
            <% } %>
          </ul>
        </div>
      </div>
    </div>
</nav>